import React,{useEffect,useMemo,useRef,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {LayoutDashboard,Plus,ClipboardList,Clock3,CheckCircle2,XCircle,Package,ShoppingCart,Tags,LogOut,Search,Bell,ChevronDown,IndianRupee,AlertTriangle,UserRound,Menu,X,Upload,Pencil,Eye,Boxes,Truck,Store,WalletCards,Save,RefreshCw} from 'lucide-react';
import './styles.css';

const CATEGORIES=['Hand-Painted Diyas','Chenille / Pipe Cleaner','Crochet','Handmade / Ready-Made Rangolis'];
const COLOURS=['Red','Green','Pink','Blue','Yellow','White','Orange','Purple'];
const PRODUCT_SEED=[
['Hand-Painted Diyas','Small Round Diya'],['Hand-Painted Diyas','Shri Diya'],['Hand-Painted Diyas','Leaf Diya'],['Hand-Painted Diyas','Classic Round Diya'],['Hand-Painted Diyas','Swastik Diya'],['Hand-Painted Diyas','Designer Flower Diya'],['Hand-Painted Diyas','Mirror Work Diya'],['Hand-Painted Diyas','Classic Candle Diya'],['Hand-Painted Diyas','Matka Candle Diya'],
['Chenille / Pipe Cleaner','Lotus Tea Light Holder'],['Chenille / Pipe Cleaner','Designer Hangings'],['Chenille / Pipe Cleaner','Flower Door Hanging'],['Chenille / Pipe Cleaner','Lotus Hanging'],['Chenille / Pipe Cleaner','Flower Toran'],
['Crochet','Small Flower Tea Light Holder'],['Crochet','Round Tea Light Holder / Coaster'],['Crochet','Designer Tea Light Holder'],['Crochet','Beautiful Flower Tea Light Holder'],['Crochet','Dual Color Tea Light Holder / Coaster'],['Crochet','Elegant Big Flower Tea Light Holder'],['Crochet','Curved Petal Tea Light Holder']
].map(([category,name],i)=>({id:'seed-'+i,category,name,imageUrl:'',sellingPricePiece:0,sellingPriceSet2:0,colours:COLOURS.join('|'),source:category==='Crochet'?'Outsourced':'In-house',active:true}));
const TEAM=[{name:'Samiksha',email:'samsknack@gmail.com'},{name:'Snehal',email:'snehalshinde302001@gmail.com'},{name:'Sandesh',email:'sandeshshinde15@gmail.com'},{name:'Anket',email:'anket.nalawade42@gmail.com'}];
const DEMO_INVENTORY=[{id:'d1',product:'Leaf Diya',category:CATEGORIES[0],colour:'Red',stock:20,minimum:10,unit:'pcs',source:'In-house'},{id:'d2',product:'Leaf Diya',category:CATEGORIES[0],colour:'Pink',stock:8,minimum:10,unit:'pcs',source:'In-house'},{id:'d3',product:'Shri Diya',category:CATEGORIES[0],colour:'Red',stock:32,minimum:10,unit:'pcs',source:'In-house'},{id:'d4',product:'Lotus Tea Light Holder',category:CATEGORIES[1],colour:'Pink',stock:25,minimum:10,unit:'pcs',source:'In-house'},{id:'d5',product:'Small Flower Tea Light Holder',category:CATEGORIES[2],colour:'Dark Pink',stock:6,minimum:10,unit:'pcs',source:'Outsourced'},{id:'d6',product:'Rangoli Set',category:CATEGORIES[3],colour:'Standard',stock:18,minimum:8,unit:'sets',source:'In-house'}];
const DEMO_PACKAGING=[{id:'p1',item:'Small Box',stock:42,minimum:50,unit:'pcs'},{id:'p2',item:'Large Box',stock:18,minimum:20,unit:'pcs'},{id:'p3',item:'Thank-you Card',stock:85,minimum:100,unit:'pcs'},{id:'p4',item:'Bubble Wrap',stock:6,minimum:3,unit:'rolls'}];
const DEMO_EXPENSES=[{id:'e1',date:'2026-09-03',expenseType:'Stall / Exhibition',item:'Diwali Stall Booking',quantity:1,unit:'booking',pricePerUnit:5000,amount:5000,allocation:'General Business',paidBy:'Samiksha'},{id:'e2',date:'2026-09-03',expenseType:'Raw Material',item:'Paint & Decorative Material',quantity:1,unit:'lot',pricePerUnit:1800,amount:1800,allocation:'Hand-Painted Diyas',paidBy:'Snehal'},{id:'e3',date:'2026-09-02',expenseType:'Outsourcing',item:'Crochet Pieces',quantity:1,unit:'lot',pricePerUnit:3000,amount:3000,allocation:'Crochet',paidBy:'Samiksha'}];

const apiBase=import.meta.env.VITE_API_BASE||'/api';
async function api(action,payload={},credential){const r=await fetch(apiBase+'/backend',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,...payload,credential})});const text=await r.text();let d;try{d=JSON.parse(text)}catch{d={ok:false,error:text||('HTTP '+r.status)}}if(!r.ok||!d.ok)throw new Error(d.error||('Request failed ('+r.status+')'));return d;}
function localId(){return crypto?.randomUUID?.()||String(Date.now());}

function App(){
 const [credential,setCredential]=useState(sessionStorage.getItem('sk_credential')||'');
 const [user,setUser]=useState(JSON.parse(sessionStorage.getItem('sk_user')||'null'));
 const [data,setData]=useState({orders:[],order_items:[],products:[],inventory:[],packaging:[],purchases_expenses:[],customers:[],team:TEAM});
 const [page,setPage]=useState('home'); const [menu,setMenu]=useState(false); const [loading,setLoading]=useState(false); const [error,setError]=useState('');
 const signedIn=!!credential&&!!user;
 const load=async(c=credential)=>{setLoading(true);setError('');try{const r=await api('list',{entity:'all'},c);setData(normalize(r.data));}catch(e){setError(e.message)}finally{setLoading(false)}};
 useEffect(()=>{if(signedIn)load()},[signedIn]);
 const login=async(cred,profile)=>{
  setLoading(true);
  setError('');
  try{
    const r=await api('bootstrap',{},cred);
    const verified=r.user||profile;
    sessionStorage.setItem('sk_credential',cred);
    sessionStorage.setItem('sk_user',JSON.stringify(verified));
    setCredential(cred);
    setUser(verified);
  }catch(e){
    sessionStorage.clear();
    setCredential('');
    setUser(null);
    setError(e.message||'This Google account is not approved for Samsknack.');
  }finally{
    setLoading(false);
  }
};
 const logout=()=>{sessionStorage.clear();setCredential('');setUser(null);setData({orders:[],order_items:[],products:[],inventory:[],packaging:[],purchases_expenses:[],customers:[],team:TEAM});setPage('home')};
 if(!signedIn)return <Login onLogin={login} error={error}/>;
 const nav=p=>{setPage(p);setMenu(false)};
 return <div className="app-shell"><Sidebar page={page} navigate={nav} open={menu} onClose={()=>setMenu(false)}/><main className="main"><header className="topbar"><button className="icon-btn mobile-menu" onClick={()=>setMenu(true)}><Menu size={20}/></button><div className="crumb">{pageLabel(page)}</div><div className="top-actions"><button className="icon-btn" onClick={()=>load()} title="Refresh"><RefreshCw size={16}/></button><button className="icon-btn"><Bell size={17}/></button><div className="user-chip"><div className="avatar">{user.name[0]}</div><span>{user.name}</span><ChevronDown size={14}/></div></div></header><div className="content">{error&&<div className="error-banner">{error}</div>}{loading&&<div className="loading-bar">Syncing with Google Sheets…</div>}
 {page==='home'&&<Dashboard user={user} data={data} navigate={nav}/>} {page==='new-order'&&<NewOrder user={user} data={data} credential={credential} onSaved={load} navigate={nav}/>} {['all-orders','pending','delivered','cancelled'].includes(page)&&<OrdersView page={page} data={data} credential={credential} onSaved={load} navigate={nav}/>} {page==='inventory'&&<Inventory data={data} credential={credential} onSaved={load}/>} {page==='packaging'&&<Packaging data={data} credential={credential} onSaved={load}/>} {page==='expenses'&&<Expenses data={data} credential={credential} onSaved={load}/>} {page==='products'&&<Products data={data} credential={credential} onSaved={load}/>} {page==='settings'&&<Settings user={user} logout={logout}/>}</div></main><MobileNav page={page} navigate={nav}/></div>
}
function normalize(d){return {orders:d.orders||[],order_items:d.order_items||[],products:(d.products&&d.products.length?d.products:PRODUCT_SEED),inventory:d.inventory||DEMO_INVENTORY,packaging:d.packaging||DEMO_PACKAGING,purchases_expenses:d.purchases_expenses||DEMO_EXPENSES,customers:d.customers||[],team:d.team||TEAM};}
function Login({onLogin,error}){const client=import.meta.env.VITE_GOOGLE_CLIENT_ID;const divRef=useRef(null);useEffect(()=>{if(!client)return;let t=setInterval(()=>{if(window.google?.accounts?.id&&divRef.current){clearInterval(t);window.google.accounts.id.initialize({client_id:client,callback:r=>{const p=parseJwt(r.credential);onLogin(r.credential,{name:p.name||p.given_name||'Team member',email:p.email})}});window.google.accounts.id.renderButton(divRef.current,{theme:'outline',size:'large',width:340,text:'continue_with'});}},100);return()=>clearInterval(t)},[client]);return <div className="login-page"><div className="login-art"><div className="brand-mark"><img src="/logo-transparent.png" alt="Sam's Knack"/></div><div className="login-copy"><div className="eyebrow">INTERNAL BUSINESS MANAGER</div><h1>Keep every order,<br/>purchase & stock<br/><em>in one place.</em></h1><p>A simple private workspace for the Samsknack team.</p></div></div><div className="login-panel"><div className="login-card"><div className="mini-logo"><img src="/logo-transparent.png" alt="Sam's Knack"/></div><div className="login-kicker">WELCOME BACK</div><h2>Sign in to Samsknack</h2><p className="login-muted">Use your approved Google account to access the internal manager.</p>{client?<div ref={divRef} className="google-holder"/>:<div className="config-box">Add <b>VITE_GOOGLE_CLIENT_ID</b> to <b>.env.local</b> first.</div>}{error&&<div className="login-error">{error}</div>}<div className="login-note">Only authorised Samsknack team accounts can access this workspace.</div><div className="team-preview">{TEAM.map(t=><span key={t.email}>{t.name}</span>)}</div></div></div></div>}
function parseJwt(token){try{return JSON.parse(atob(token.split('.')[1].replace(/-/g,'+').replace(/_/g,'/')))}catch{return {}}}
function Sidebar({page,navigate,open,onClose}){const item=(id,label,I)=><button className={'nav-item '+(page===id?'active':'')} onClick={()=>navigate(id)}><I size={18}/><span>{label}</span></button>;return <>{open&&<div className="sidebar-overlay" onClick={onClose}/>}<aside className={'sidebar '+(open?'mobile-open':'')}><div className="sidebar-brand"><img src="/logo-transparent.png" alt="Sam's Knack"/></div><div className="nav-scroll"><div className="nav-section">WORKSPACE</div>{item('home','Home',LayoutDashboard)}<div className="nav-section">ORDERS</div>{item('new-order','New Order',Plus)}{item('all-orders','All Orders',ClipboardList)}{item('pending','Pending Orders',Clock3)}{item('delivered','Delivered Orders',CheckCircle2)}{item('cancelled','Cancelled / Damaged',XCircle)}<div className="nav-section">STOCK & SPEND</div>{item('inventory','Product Inventory',Package)}{item('packaging','Packaging Inventory',Boxes)}{item('expenses','Purchases & Expenses',ShoppingCart)}<div className="nav-section">CATALOGUE</div>{item('products','Products',Tags)}<div className="nav-section">SYSTEM</div>{item('settings','Settings',UserRound)}</div><div className="sidebar-foot">SK · Internal use only</div></aside></>}
function MobileNav({page,navigate}){return <nav className="mobile-bottom-nav"><button className={page==='home'?'active':''} onClick={()=>navigate('home')}><LayoutDashboard size={19}/><span>Home</span></button><button className={page==='new-order'?'active':''} onClick={()=>navigate('new-order')}><Plus size={21}/><span>New Order</span></button><button className={['all-orders','pending','delivered','cancelled'].includes(page)?'active':''} onClick={()=>navigate('all-orders')}><ClipboardList size={19}/><span>Orders</span></button><button className={['inventory','packaging'].includes(page)?'active':''} onClick={()=>navigate('inventory')}><Package size={19}/><span>Stock</span></button><button onClick={()=>navigate('settings')}><Menu size={19}/><span>More</span></button></nav>}
function Dashboard({user,data,navigate}){const orders=data.orders;const active=orders.filter(o=>!['Delivered','Cancelled / Damaged'].includes(o.status)).length;const deliver=orders.filter(o=>!['Delivered','Cancelled / Damaged'].includes(o.status)).length;const low=data.inventory.filter(i=>Number(i.stock)<=Number(i.minimum)).length+data.packaging.filter(i=>Number(i.stock)<=Number(i.minimum)).length;const revenue=orders.reduce((s,o)=>s+Number(o.totalAmount||0),0);const exp=data.purchases_expenses.reduce((s,e)=>s+Number(e.amount||0),0);const profit=revenue-exp;return <><div className="page-heading"><div><div className="eyebrow">DASHBOARD</div><h1>Hi, {user.name} 👋</h1><p>Welcome back. Here's your Samsknack overview.</p></div><button className="primary-btn" onClick={()=>navigate('new-order')}><Plus size={18}/> New Order</button></div><div className="metric-grid"><Metric icon={ClipboardList} label="Orders to Make" value={active} note="Active orders"/><Metric icon={Truck} label="Orders to Deliver" value={deliver} note="Not yet delivered"/><Metric icon={AlertTriangle} label="Low Stock Items" value={low} note="Need attention"/><Metric icon={IndianRupee} label="Estimated Profit" value={'₹'+profit.toLocaleString('en-IN')} note="From recorded sales & spend"/></div><div className="dashboard-grid"><section className="panel chart-panel"><PanelTitle title="Order Status" subtitle="Current order pipeline"/><div className="bar-chart">{['Pending','In Production','Ready','Delivered'].map(s=>{const v=orders.filter(o=>o.status===s).length;return <div className="bar-row" key={s}><div className="bar-label"><span>{s}</span><b>{v}</b></div><div className="bar-track"><div className="bar-fill" style={{width:`${Math.max(v?8:0,v/Math.max(1,orders.length)*100)}%`}}/></div></div>})}</div></section><section className="panel"><PanelTitle title="Inventory Snapshot" subtitle="Stock by category"/><div className="category-list">{CATEGORIES.map((c,i)=>{const count=data.inventory.filter(x=>x.category===c).reduce((s,x)=>s+Number(x.stock||0),0);return <div className="category-row" key={c}><div className="cat-icon">{i===0?'🪔':i===1?'🌸':i===2?'🧶':'🌼'}</div><div className="cat-info"><strong>{c}</strong><span>{count} units</span></div><div className="cat-value">{count}</div></div>})}</div></section></div><section className="panel profit-panel"><PanelTitle title="Business Overview" subtitle="Sales, expenses and estimated profit"/><div className="profit-grid"><div><span>Order Revenue</span><strong>₹{revenue.toLocaleString('en-IN')}</strong></div><div><span>Purchases & Expenses</span><strong>₹{exp.toLocaleString('en-IN')}</strong></div><div><span>Estimated Profit</span><strong className="profit-value">₹{profit.toLocaleString('en-IN')}</strong></div></div></section></>}
function Metric({icon:Icon,label,value,note}){return <div className="metric-card"><div className="metric-icon"><Icon size={19}/></div><div><span>{label}</span><strong>{value}</strong><small>{note}</small></div></div>}
function PanelTitle({title,subtitle}){return <div className="panel-title"><div><h3>{title}</h3><span>{subtitle}</span></div></div>}

function NewOrder({user,data,credential,onSaved,navigate}){
 const [name,setName]=useState(''),[phone,setPhone]=useState(''),[delivery,setDelivery]=useState(''),[notes,setNotes]=useState(''),[paid,setPaid]=useState(0),[payment,setPayment]=useState('Not Paid'),[found,setFound]=useState(null),[saving,setSaving]=useState(false),[items,setItems]=useState([blankItem(data.products)]);
 const suggestions=useMemo(()=>{const n=name.trim().toLowerCase(),p=phone.trim();if(!n&&!p)return[];return data.orders.filter(o=>(n&&String(o.customer||'').toLowerCase().includes(n))||(p&&String(o.phone||'').includes(p))).slice(0,4)},[name,phone,data.orders]);
 const totalPieces=items.reduce((s,x)=>s+Number(x.quantity||0)*Number(x.piecesPerUnit||1),0);const bulk=totalPieces>50;
 const update=(idx,key,val)=>setItems(a=>a.map((x,i)=>i===idx?({...x,[key]:val}):x));
 const addLine=()=>setItems(a=>[...a,blankItem(data.products)]); const removeLine=i=>setItems(a=>a.filter((_,j)=>j!==i));
 const submit=async e=>{e.preventDefault();const normalized=items.map(x=>({...x,piecesPerUnit:x.sellingUnit==='Set of 2'?2:1,unitPrice:Number(x.unitPrice||0),quantity:Number(x.quantity||0),lineTotal:Number(x.unitPrice||0)*Number(x.quantity||0)}));if(normalized.some(x=>!x.product||!x.colour||x.quantity<1)){alert('Please complete product, quantity and colour for every product.');return}if(normalized.some(x=>x.customizationRequired&&!String(x.customizationDetails||'').trim())){alert('Customization details are required when customization is selected.');return}if(Number(paid||0)>normalized.reduce((s,x)=>s+x.lineTotal,0)){alert('Paid amount cannot be greater than the total order amount.');return}setSaving(true);try{const payload={order:{customer:name,phone,delivery,notes,paidAmount:Number(paid||0),paymentStatus:payment,status:'Pending',items:normalized}};await api('createOrder',payload,credential);await onSaved();navigate('all-orders')}catch(e){alert(e.message)}finally{setSaving(false)}};
 return <div><div className="page-heading"><div><div className="eyebrow">ORDERS</div><h1>New Order</h1><p>Enter the customer once, then add as many products as needed.</p></div></div><form className="order-layout" onSubmit={submit}><section className="panel auto-type-banner"><div className="auto-type-icon">{bulk?'📦':'🛍️'}</div><div><span className="auto-type-kicker">ORDER TYPE · AUTOMATIC</span><strong>{bulk?'Bulk Order':'Personal Order'}</strong><p>{totalPieces} actual pieces · {bulk?'51+ pieces → SKC':'50 pieces or less → Personal'}</p></div>{bulk&&<span className="skc-badge">SKC</span>}</section><section className="panel form-panel"><PanelTitle title="Customer Details" subtitle="Existing orders are suggested while you type"/><div className="form-grid"><label>Customer Name<input value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Varsha Patwardhan" required/></label><label>Phone Number<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="10-digit number" required/></label></div>{suggestions.length>0&&<div className="suggestions"><div className="suggestion-head">Existing orders found</div>{suggestions.map(o=><button type="button" className="suggestion" key={o.id} onClick={()=>{setFound(o);setName(o.customer);setPhone(o.phone)}}><div className="avatar small">{String(o.customer||'?')[0]}</div><div><strong>{o.customer}</strong><span>{o.code?o.code+' · ':''}{formatDate(o.delivery)} · {o.status}</span></div><Eye size={16}/></button>)}</div>}{found&&<div className="found-box"><CheckCircle2 size={18}/><div><strong>{found.customer}</strong><p>{formatDate(found.delivery)} · {found.status}</p></div><button type="button" onClick={()=>navigate('all-orders')}>Open orders</button></div>}<div className="form-grid one-three"><label>Delivery Date<input type="date" value={delivery} onChange={e=>setDelivery(e.target.value)} required/></label><label>Notes<textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder="General order notes..."/></label></div></section>
 {items.map((it,idx)=><ProductLine key={it._key} index={idx} item={it} products={data.products} update={update} remove={removeLine} canRemove={items.length>1}/>) }
 <section className="panel form-panel"><PanelTitle title="Payment" subtitle="Keep it simple — the app calculates the balance"/><div className="form-grid"><label>Total Amount<input value={'₹'+items.reduce((s,x)=>s+Number(x.unitPrice||0)*Number(x.quantity||0),0).toLocaleString('en-IN')} readOnly/></label><label>Paid Amount<input type="number" min="0" value={paid} onChange={e=>setPaid(e.target.value)}/></label><label>Payment Status<select value={payment} onChange={e=>setPayment(e.target.value)}><option>Not Paid</option><option>Partially Paid</option><option>Paid</option></select></label></div></section><div className="add-line-wrap"><button type="button" className="outline-btn add-line" onClick={addLine}><Plus size={16}/> Add Another Product</button></div><div className="submit-row"><span><b>{totalPieces}</b> actual pieces · {bulk?'Bulk / SKC':'Personal'}</span><button className="primary-btn" disabled={saving}><Save size={17}/> {saving?'Saving…':'Save Order'}</button></div></form></div>}
function blankItem(products){const p=products?.[0]||PRODUCT_SEED[0];return {_key:localId(),category:p.category,product:p.name,productImage:p.imageUrl||'',sellingUnit:'Per Piece',piecesPerUnit:1,quantity:1,colour:'',customizationRequired:false,customizationDetails:'',referenceImageUrl:'',unitPrice:Number(p.sellingPricePiece||0)}}
function ProductLine({index,item,products,update,remove,canRemove}){const catProducts=products.filter(p=>p.category===item.category);const selected=products.find(p=>p.name===item.product&&p.category===item.category)||catProducts[0]||products[0];const colours=(selected?.colours?String(selected.colours).split('|').filter(Boolean):COLOURS);const onCat=v=>{const p=products.find(x=>x.category===v)||products[0];update(index,'category',v);update(index,'product',p?.name||'');update(index,'productImage',p?.imageUrl||'');update(index,'unitPrice',Number(p?.sellingPricePiece||0));update(index,'colour','')};const onProd=v=>{const p=products.find(x=>x.name===v&&x.category===item.category);update(index,'product',v);update(index,'productImage',p?.imageUrl||'');update(index,'unitPrice',Number(item.sellingUnit==='Set of 2'?p?.sellingPriceSet2||0:p?.sellingPricePiece||0));update(index,'colour','')};const upload=async e=>{const f=e.target.files?.[0];if(!f)return;update(index,'_uploading',true);try{const data=await fileData(f);const r=await api('uploadImage',{file:{name:f.name,mimeType:f.type,data}},window.sessionStorage.getItem('sk_credential'));update(index,'referenceImageUrl',r.data.url)}catch(err){alert(err.message)}finally{update(index,'_uploading',false)}};return <section className="panel form-panel"><div className="line-heading"><div><div className="line-title">Product {index+1}</div><span>Select from the catalogue</span></div>{canRemove&&<button type="button" className="icon-btn" onClick={()=>remove(index)}><X size={16}/></button>}</div><div className="product-selector"><div className="product-image-box">{selected?.imageUrl?<img src={selected.imageUrl} alt={selected.name}/>:<div className="product-placeholder"><Tags size={30}/><span>Catalogue image</span><small>Add it from Products</small></div>}</div><div className="product-fields"><label>Category<select value={item.category} onChange={e=>onCat(e.target.value)}>{CATEGORIES.map(c=><option key={c}>{c}</option>)}</select></label><label>Product<select value={item.product} onChange={e=>onProd(e.target.value)}>{catProducts.map(p=><option key={p.id}>{p.name}</option>)}</select></label><div className="form-grid"><label>Quantity<input type="number" min="1" value={item.quantity} onChange={e=>update(index,'quantity',Number(e.target.value))}/></label><label>Sold As<select value={item.sellingUnit} onChange={e=>{const v=e.target.value;update(index,'sellingUnit',v);update(index,'piecesPerUnit',v==='Set of 2'?2:1);update(index,'unitPrice',Number(v==='Set of 2'?selected?.sellingPriceSet2||0:selected?.sellingPricePiece||0))}}><option>Per Piece</option><option>Set of 2</option></select></label></div><label>Colour<select value={item.colour} onChange={e=>update(index,'colour',e.target.value)} required><option value="">Select colour</option>{colours.map(c=><option key={c}>{c}</option>)}</select></label><label>Unit Price<input type="number" min="0" value={item.unitPrice} onChange={e=>update(index,'unitPrice',Number(e.target.value))}/><small className="field-note">Price is for the selected selling option.</small></label></div></div><div className="custom-row"><label className="check-label"><input type="checkbox" checked={item.customizationRequired} onChange={e=>update(index,'customizationRequired',e.target.checked)}/> Customization required</label>{item.customizationRequired&&<div className="custom-fields"><label>Customization Details *<textarea required value={item.customizationDetails} onChange={e=>update(index,'customizationDetails',e.target.value)} placeholder="Enter exactly what the customer wants..."/></label><label className="upload-box"><Upload size={15}/>{item._uploading?'Uploading…':item.referenceImageUrl?'Reference image added':'Add reference image (optional)'}<input type="file" accept="image/*" onChange={upload}/></label></div>}</div></section>}
function fileData(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(file)})}

function OrdersView({page,data,credential,onSaved,navigate}){const [q,setQ]=useState(''),[type,setType]=useState('All'),[cat,setCat]=useState('All Categories'),[status,setStatus]=useState('All Statuses');const wanted=page==='pending'?data.orders.filter(o=>!['Delivered','Cancelled / Damaged'].includes(o.status)):page==='delivered'?data.orders.filter(o=>o.status==='Delivered'):page==='cancelled'?data.orders.filter(o=>o.status==='Cancelled / Damaged'):data.orders;const filtered=wanted.filter(o=>{const text=(q+' '+(o.customer||'')+' '+(o.phone||'')+' '+(o.code||'')).toLowerCase();const items=data.order_items.filter(i=>String(i.orderId)===String(o.id));const cats=[...(o.categories||[]),...items.map(i=>i.category)];return text.includes(q.toLowerCase())&&(type==='All'||o.type===type)&&(cat==='All Categories'||cats.includes(cat))&&(status==='All Statuses'||o.status===status)});const change=async(o,s)=>{try{await api('updateOrder',{order:{id:o.id,status:s}},credential);await onSaved()}catch(e){alert(e.message)}};return <div><div className="page-heading"><div><div className="eyebrow">ORDERS</div><h1>{page==='all-orders'?'All Orders':page==='pending'?'Pending Orders':page==='delivered'?'Delivered Orders':'Cancelled / Damaged Orders'}</h1><p>Search and update orders from any phone.</p></div><button className="primary-btn" onClick={()=>navigate('new-order')}><Plus size={18}/> New Order</button></div><section className="panel table-panel"><div className="filters"><div className="search"><Search size={17}/><input placeholder="Search customer, phone or SKC..." value={q} onChange={e=>setQ(e.target.value)}/></div><select value={type} onChange={e=>setType(e.target.value)}><option>All</option><option>Personal</option><option>Bulk</option></select><select value={cat} onChange={e=>setCat(e.target.value)}><option>All Categories</option>{CATEGORIES.map(c=><option key={c}>{c}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)}><option>All Statuses</option>{['Pending','In Production','Ready','Delivered','Cancelled / Damaged'].map(s=><option key={s}>{s}</option>)}</select></div><div className="desktop-table"><div className="table-wrap"><table><thead><tr><th>Customer</th><th>Type</th><th>Category</th><th>Items</th><th>Delivery</th><th>Status</th><th>Payment</th></tr></thead><tbody>{filtered.map(o=><OrderRow key={o.id} o={o} data={data} change={change}/>)}</tbody></table></div></div><div className="mobile-order-list">{filtered.map(o=><div className="mobile-order-card" key={o.id}><div className="mobile-order-top"><div className="customer-cell"><div className="avatar small">{String(o.customer||'?')[0]}</div><div><strong>{o.customer}</strong><span>{o.phone}</span></div></div><span className={'type-pill '+String(o.type||'').toLowerCase()}>{o.type}</span></div><div className="mobile-order-item">{itemSummary(o,data)}</div><div className="mobile-order-meta"><span><b>Category</b>{categorySummary(o,data)}</span><span><b>Delivery</b>{formatDate(o.delivery)}</span><span><b>Payment</b>{o.paymentStatus||o.payment||'Not Paid'}</span><span><b>Amount</b>₹{Number(o.totalAmount||o.total||0).toLocaleString('en-IN')}</span></div><div className="mobile-order-actions"><select className={'status-select '+statusClass(o.status)} value={o.status} onChange={e=>change(o,e.target.value)}>{statusOptions()}</select></div></div>)}{filtered.length===0&&<div className="empty">No orders found.</div>}</div></section></div>}
function OrderRow({o,data,change}){return <tr><td><div className="customer-cell"><div className="avatar small">{String(o.customer||'?')[0]}</div><div><strong>{o.customer}</strong><span>{o.phone}</span></div></div></td><td><span className={'type-pill '+String(o.type||'').toLowerCase()}>{o.type}</span>{o.code&&<span className="order-code">{o.code}</span>}</td><td>{categorySummary(o,data)}</td><td>{itemSummary(o,data)}</td><td>{formatDate(o.delivery)}</td><td><select className={'status-select '+statusClass(o.status)} value={o.status} onChange={e=>change(o,e.target.value)}>{statusOptions()}</select></td><td>{o.paymentStatus||'Not Paid'}</td></tr>}
function statusOptions(){return ['Pending','In Production','Ready','Delivered','Cancelled / Damaged'].map(s=><option key={s}>{s}</option>)}
function orderItems(o,data){return data.order_items.filter(i=>String(i.orderId)===String(o.id))}
function itemSummary(o,data){const its=orderItems(o,data);if(its.length)return its.map(i=>`${i.quantity} × ${i.product} (${i.sellingUnit})${i.colour?' · '+i.colour:''}`).join(' · ');return o.items||'—'}
function categorySummary(o,data){const cats=[...(o.categories||[]),...orderItems(o,data).map(i=>i.category)];return [...new Set(cats)].filter(Boolean).map(c=><span className="category-pill" key={c}>{shortCategory(c)}</span>)}

function Inventory({data,credential,onSaved}){const [cat,setCat]=useState('All Categories');const [open,setOpen]=useState(false);const [form,setForm]=useState({category:CATEGORIES[0],product:'',colour:'',stock:0,minimum:0,unit:'pcs',source:'In-house'});const list=data.inventory.filter(i=>cat==='All Categories'||i.category===cat);const save=async()=>{try{await api('createInventory',{item:form},credential);setOpen(false);await onSaved()}catch(e){alert(e.message)}};return <div><div className="page-heading"><div><div className="eyebrow">INVENTORY</div><h1>Product Inventory</h1><p>Track stock down to product + colour/variant.</p></div><button className="primary-btn" onClick={()=>setOpen(true)}><Plus size={18}/> Add Stock</button></div><div className="category-tabs">{['All Categories',...CATEGORIES].map(c=><button className={cat===c?'active':''} key={c} onClick={()=>setCat(c)}>{c}</button>)}</div><section className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>Product</th><th>Category</th><th>Colour / Variant</th><th>Stock</th><th>Minimum</th><th>Need to Order</th><th>Status</th></tr></thead><tbody>{list.map(i=>{const need=Math.max(0,Number(i.minimum)-Number(i.stock));return <tr key={i.id}><td><strong>{i.product}</strong></td><td>{shortCategory(i.category)}</td><td>{i.colour}</td><td>{i.stock} {i.unit}</td><td>{i.minimum}</td><td><b>{need}</b></td><td>{need?<span className="stock-low">Low Stock</span>:<span className="stock-good">Healthy</span>}</td></tr>})}</tbody></table></div></section>{open&&<Modal title="Add Product Stock" close={()=>setOpen(false)}><SimpleInventory form={form} setForm={setForm} data={data}/><button className="primary-btn full" onClick={save}><Save size={16}/> Save Stock</button></Modal>}</div>}
function SimpleInventory({form,setForm,data}){const ps=data.products.filter(p=>p.category===form.category);return <div className="modal-form"><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value,product:''})}>{CATEGORIES.map(c=><option key={c}>{c}</option>)}</select></label><label>Product<select value={form.product} onChange={e=>setForm({...form,product:e.target.value})}><option value="">Select product</option>{ps.map(p=><option key={p.id}>{p.name}</option>)}</select></label><label>Colour / Variant<select value={form.colour} onChange={e=>setForm({...form,colour:e.target.value})}><option value="">Select</option>{COLOURS.map(c=><option key={c}>{c}</option>)}</select></label><label>Stock<input type="number" value={form.stock} onChange={e=>setForm({...form,stock:Number(e.target.value)})}/></label><label>Minimum Stock<input type="number" value={form.minimum} onChange={e=>setForm({...form,minimum:Number(e.target.value)})}/></label><label>Unit<input value={form.unit} onChange={e=>setForm({...form,unit:e.target.value})}/></label></div>}
function Packaging({data,credential,onSaved}){const [open,setOpen]=useState(false),[form,setForm]=useState({item:'',stock:0,minimum:0,unit:'pcs'});const save=async()=>{try{await api('createPackaging',{item:form},credential);setOpen(false);await onSaved()}catch(e){alert(e.message)}};return <div><div className="page-heading"><div><div className="eyebrow">INVENTORY</div><h1>Packaging Inventory</h1><p>Track boxes, cards, wrap and other packaging.</p></div><button className="primary-btn" onClick={()=>setOpen(true)}><Plus size={18}/> Add Packaging</button></div><section className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>Item</th><th>Stock</th><th>Unit</th><th>Minimum</th><th>Need to Order</th><th>Status</th></tr></thead><tbody>{data.packaging.map(i=>{const n=Math.max(0,Number(i.minimum)-Number(i.stock));return <tr key={i.id}><td><strong>{i.item}</strong></td><td>{i.stock}</td><td>{i.unit}</td><td>{i.minimum}</td><td>{n}</td><td>{n?<span className="stock-low">Restock</span>:<span className="stock-good">Healthy</span>}</td></tr>})}</tbody></table></div></section>{open&&<Modal title="Add Packaging" close={()=>setOpen(false)}><div className="modal-form"><label>Item<input value={form.item} onChange={e=>setForm({...form,item:e.target.value})}/></label><label>Stock<input type="number" value={form.stock} onChange={e=>setForm({...form,stock:Number(e.target.value)})}/></label><label>Minimum<input type="number" value={form.minimum} onChange={e=>setForm({...form,minimum:Number(e.target.value)})}/></label><label>Unit<input value={form.unit} onChange={e=>setForm({...form,unit:e.target.value})}/></label></div><button className="primary-btn full" onClick={save}><Save size={16}/> Save</button></Modal>}</div>}
function Expenses({data,credential,onSaved}){const total=data.purchases_expenses.reduce((s,e)=>s+Number(e.amount||0),0),[open,setOpen]=useState(false),[form,setForm]=useState({date:new Date().toISOString().slice(0,10),expenseType:'Raw Material',item:'',quantity:1,unit:'pcs',pricePerUnit:0,allocation:'General Business',supplier:'',paymentStatus:'Paid',notes:''});const save=async()=>{try{await api('createExpense',{item:form},credential);setOpen(false);await onSaved()}catch(e){alert(e.message)}};return <div><div className="page-heading"><div><div className="eyebrow">FINANCE</div><h1>Purchases & Expenses</h1><p>Record everything you spend so profit can be estimated.</p></div><button className="primary-btn" onClick={()=>setOpen(true)}><Plus size={18}/> Add Expense</button></div><div className="metric-grid three"><Metric icon={ShoppingCart} label="Total Recorded" value={'₹'+total.toLocaleString('en-IN')} note="All recorded spend"/><Metric icon={Store} label="Stall / Exhibition" value={'₹'+data.purchases_expenses.filter(e=>e.expenseType==='Stall / Exhibition').reduce((s,e)=>s+Number(e.amount||0),0).toLocaleString('en-IN')} note="Business expense"/><Metric icon={WalletCards} label="Expense Entries" value={data.purchases_expenses.length} note="Recorded purchases & spend"/></div><section className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>Date</th><th>Expense</th><th>Type</th><th>Allocation</th><th>Amount</th><th>Paid By</th></tr></thead><tbody>{data.purchases_expenses.map(e=><tr key={e.id}><td>{formatDate(e.date)}</td><td><strong>{e.item}</strong></td><td>{e.expenseType}</td><td><span className="category-pill">{e.allocation}</span></td><td><strong>₹{Number(e.amount||0).toLocaleString('en-IN')}</strong></td><td>{e.paidBy||'—'}</td></tr>)}</tbody></table></div></section>{open&&<Modal title="Add Purchase / Expense" close={()=>setOpen(false)}><div className="modal-form"><label>Date<input type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></label><label>Expense Type<select value={form.expenseType} onChange={e=>setForm({...form,expenseType:e.target.value})}>{['Raw Material','Finished Products','Packaging','Outsourcing','Transportation','Delivery','Stall / Exhibition','Marketing','Equipment / Tools','Other Business Expense'].map(x=><option key={x}>{x}</option>)}</select></label><label>What was bought / paid for<input value={form.item} onChange={e=>setForm({...form,item:e.target.value})}/></label><label>Quantity<input type="number" value={form.quantity} onChange={e=>setForm({...form,quantity:Number(e.target.value)})}/></label><label>Price per Unit<input type="number" value={form.pricePerUnit} onChange={e=>setForm({...form,pricePerUnit:Number(e.target.value)})}/></label><label>Allocation<select value={form.allocation} onChange={e=>setForm({...form,allocation:e.target.value})}>{['General Business',...CATEGORIES,'Specific Order'].map(x=><option key={x}>{x}</option>)}</select></label><label>Supplier<input value={form.supplier} onChange={e=>setForm({...form,supplier:e.target.value})}/></label><label>Notes<textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label></div><button className="primary-btn full" onClick={save}><Save size={16}/> Save Expense</button></Modal>}</div>}

function Products({data,credential,onSaved}){const [cat,setCat]=useState('All Categories'),[open,setOpen]=useState(false),[form,setForm]=useState({category:CATEGORIES[0],name:'',sellingPricePiece:0,sellingPriceSet2:0,colours:COLOURS,source:'In-house'});const visible=data.products.filter(p=>cat==='All Categories'||p.category===cat);const save=async()=>{try{await api('createProduct',{product:{...form,colours:form.colours}} ,credential);setOpen(false);await onSaved()}catch(e){alert(e.message)}};const upload=async(p)=>{const input=document.createElement('input');input.type='file';input.accept='image/*';input.onchange=async()=>{const f=input.files?.[0];if(!f)return;try{const r=await api('uploadImage',{file:{name:f.name,mimeType:f.type,data:await fileData(f)}},credential);await api('updateProduct',{product:{id:p.id,imageUrl:r.data.url}},credential);await onSaved()}catch(e){alert(e.message)}};input.click()};return <div><div className="page-heading"><div><div className="eyebrow">CATALOGUE</div><h1>Products</h1><p>Add one image per product so order punching is visual and easy.</p></div><button className="primary-btn" onClick={()=>setOpen(true)}><Plus size={18}/> Add Product</button></div><div className="category-tabs">{['All Categories',...CATEGORIES].map(c=><button className={cat===c?'active':''} key={c} onClick={()=>setCat(c)}>{c}</button>)}</div><div className="product-grid">{visible.map(p=><div className="catalog-card" key={p.id}>{p.imageUrl?<img className="catalog-image" src={p.imageUrl} alt={p.name}/>:<div className="catalog-image placeholder"><Tags size={27}/><span>No image yet</span></div>}<div className="catalog-body"><strong>{p.name}</strong><span>{shortCategory(p.category)} · {p.source||'In-house'}</span><button className="outline-btn" onClick={()=>upload(p)}><Upload size={15}/> {p.imageUrl?'Replace Image':'Add Image'}</button></div></div>)}</div>{open&&<Modal title="Add Product" close={()=>setOpen(false)}><div className="modal-form"><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{CATEGORIES.map(c=><option key={c}>{c}</option>)}</select></label><label>Product Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Per Piece Price<input type="number" value={form.sellingPricePiece} onChange={e=>setForm({...form,sellingPricePiece:Number(e.target.value)})}/></label><label>Set of 2 Price<input type="number" value={form.sellingPriceSet2} onChange={e=>setForm({...form,sellingPriceSet2:Number(e.target.value)})}/></label><label>Colours<select multiple value={form.colours} onChange={e=>setForm({...form,colours:[...e.target.selectedOptions].map(x=>x.value)})}>{COLOURS.map(c=><option key={c}>{c}</option>)}</select></label><label>Source<select value={form.source} onChange={e=>setForm({...form,source:e.target.value})}><option>In-house</option><option>Outsourced</option></select></label></div><button className="primary-btn full" onClick={save}><Save size={16}/> Save Product</button></Modal>}</div>}
function Modal({title,close,children}){return <div className="modal-backdrop"><div className="modal"><div className="modal-head"><h3>{title}</h3><button className="icon-btn" onClick={close}><X size={17}/></button></div>{children}</div></div>}
function Settings({user,logout}){return <div><div className="page-heading"><div><div className="eyebrow">SYSTEM</div><h1>Settings</h1><p>Approved team access and your current account.</p></div></div><section className="panel settings-panel"><PanelTitle title="Signed-in account" subtitle="Authenticated with Google"><div/></PanelTitle><div className="account-row"><div className="avatar large">{user.name[0]}</div><div><strong>{user.name}</strong><span>{user.email}</span></div></div></section><section className="panel settings-panel"><PanelTitle title="Approved Samsknack accounts" subtitle="Access is enforced by Apps Script"/><div>{TEAM.map(t=><div className="team-row" key={t.email}><div className="avatar small">{t.name[0]}</div><div><strong>{t.name}</strong><span>{t.email}</span></div><span className="access">Approved</span></div>)}</div></section><button className="danger-btn" onClick={logout}><LogOut size={17}/> Sign out</button></div>}
function pageLabel(p){return ({home:'Home','new-order':'New Order','all-orders':'All Orders',pending:'Pending Orders',delivered:'Delivered Orders',cancelled:'Cancelled / Damaged',inventory:'Product Inventory',packaging:'Packaging Inventory',expenses:'Purchases & Expenses',products:'Products',settings:'Settings'})[p]||'Samsknack'}
function shortCategory(c){return String(c||'').replace('Hand-Painted ','').replace('Handmade / Ready-Made ','').replace(' / Pipe Cleaner','')}
function statusClass(s){return String(s||'').toLowerCase().replaceAll(' ','-').replaceAll('/','')}
function formatDate(d){if(!d)return '—';const x=new Date(String(d).slice(0,10)+'T00:00:00');return isNaN(x)?String(d):x.toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'})}

createRoot(document.getElementById('root')).render(<App/>);
